---
framework: mocha
postmerge:
  pattern:
    - "./test/**/*.spec.ts"
  strategy:
    threshold: <5|100s|30m|6h>
    type: <commit|time>
premerge:
  - "./test/**/*.spec.ts"
prerun:
  - npm ci
postrun:
  - node --version
version: 2.0
